; Password manager helper hotkey.
; Enable by adding "40-password-manager" to dotkeys_config.toml.

^!b::
{
    password := EnvGet("PWMANAGER_PASSWORD")
    if (password = "") {
        MsgBox "PWMANAGER_PASSWORD environment variable is not set."
        return
    }

    SendText(password)
    Send("{Enter}")
}
